fastapi==0.136.3
uvicorn[standard]==0.32.0
# starlette pinned to 0.49.1 (latest pre-1.0); cannot go to 1.0.1 until
# prometheus-fastapi-instrumentator drops its starlette<1.0.0 constraint.
# Tracking PYSEC-2026-161 as accepted risk (low — affects multipart parsing).
starlette==0.49.1
prometheus-fastapi-instrumentator==7.0.0
python-json-logger==2.0.7
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.0.0
httpx==0.28.1
python-dotenv==1.2.2
requests==2.33.0
